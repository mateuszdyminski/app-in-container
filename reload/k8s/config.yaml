apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Config could be pair: key-value
  message: Hello Wroclaw!
  foo: bar
  
  # Or set as complete file contents
  keys: |
    message=Hello World 
    foo=bar